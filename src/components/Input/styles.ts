import { styled } from 'styled-components/native';
import { v2 } from '@creditas/mobile-tokens';

export const CustomInput = styled.TextInput`
  padding: ${v2.spacing.Y.lg};
  margin: ${v2.spacing.Y.lg};
  width: 50%;
  border-bottom-width: 1px;
  border-color: #ccc;
`;
